apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: inventory-batch
  description: Sample service with CI/CD
  annotations:
    devlake.io/project-name: inventory-batch
    github.com/project-slug: devops-champ/inventory-batch
    backstage.io/techdocs-ref: dir:.
    sonarqube.org/project-key: devops-champ_inventory-batch
    roadie-backstage-pull-requests/default-filter: "is:open is:pr"
    platform.io/argocd-dev: inventory-batch-dev
    platform.io/argocd-qa: inventory-batch-qa
    platform.io/argocd-prod: inventory-batch-prod
    grafana/log-namespace: inventory-batch-dev
spec:
  type: service
  lifecycle: experimental
  owner: group:default/platform-team
  dependsOn:
    - resource:default/orders-db
    - resource:default/kafka-cluster
  platform:
    AppId: INT-125
    ApprovedJiraId: JIRA-125
    BusinessUnit: Marketing
    SupportTeam: DEVOPS
    CloudBudgetTicket: CR-125
  infrastructure:
    environments:
      - name: dev
        projectId: devops-champ
      - name: qa
        projectId: devops-champ
      - name: prod  
        projectId: devops-champ